function [rho_skin, base_imp] = import_model(filename, dataLines)
%IMPORTFILE1 Import data from a text file
%  [RHO_SKIN, BASE_IMP] = IMPORTFILE1(FILENAME) reads data from text
%  file FILENAME for the default selection.  Returns the data as column
%  vectors.
%
%  [RHO_SKIN, BASE_IMP] = IMPORTFILE1(FILE, DATALINES) reads data for
%  the specified row interval(s) of text file FILENAME. Specify
%  DATALINES as a positive scalar integer or a N-by-2 array of positive
%  scalar integers for dis-contiguous row intervals.
%
%  Example:
%  [rho_skin, base_imp] = importfile1("D:\МГТУ аспа\эксперименты\G (2)_1\DATA\2025-04-18\model_res.txt", [6, Inf]);
%
%  See also READTABLE.
%
% Auto-generated by MATLAB on 10-May-2025 10:42:10

%% Input handling

% If dataLines is not specified, define defaults
if nargin < 2
    dataLines = [6, Inf];
end

%% Set up the Import Options and import the data
opts = delimitedTextImportOptions("NumVariables", 2);

% Specify range and delimiter
opts.DataLines = dataLines;
opts.Delimiter = " ";

% Specify column names and types
opts.VariableNames = ["rho_skin", "base_imp"];
opts.VariableTypes = ["double", "double"];

% Specify file level properties
opts.ExtraColumnsRule = "ignore";
opts.EmptyLineRule = "read";
opts.ConsecutiveDelimitersRule = "join";
opts.LeadingDelimitersRule = "ignore";

% Import the data
tbl = readtable(filename, opts);

%% Convert to output type
rho_skin = tbl.rho_skin;
base_imp = tbl.base_imp;
end