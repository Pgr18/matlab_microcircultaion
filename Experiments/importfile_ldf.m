function tbl = importfile_ldf(filename, dataLines)
%IMPORTFILE1 Import data from a text file
%  [MC, TIME, DATE, START_TIME] = IMPORTFILE1(FILENAME) reads data from
%  text file FILENAME for the default selection.  Returns the data as
%  column vectors.
%
%  [MC, TIME, DATE, START_TIME] = IMPORTFILE1(FILE, DATALINES) reads
%  data for the specified row interval(s) of text file FILENAME. Specify
%  DATALINES as a positive scalar integer or a N-by-2 array of positive
%  scalar integers for dis-contiguous row intervals.
%
%  Example:
%  [MC, Time, Date, Start_time] = importfile1("D:\МГТУ аспа\эксперименты\G (2)_1\DATA\2025-01-24\Лазма\F_01.txt", [1, Inf]);
%
%  See also READTABLE.
%
% Auto-generated by MATLAB on 01-Feb-2025 19:17:09

%% Input handling

% If dataLines is not specified, define defaults
if nargin < 2
    dataLines = [1, Inf];
end

%% Set up the Import Options and import the data
opts = delimitedTextImportOptions("NumVariables", 6);

% Specify range and delimiter
opts.DataLines = dataLines;
opts.Delimiter = " ";

% Specify column names and types
opts.VariableNames = ["MC", "Time", "Var3", "Var4", "Date", "Start_time"];
opts.SelectedVariableNames = ["MC", "Time", "Date", "Start_time"];
opts.VariableTypes = ["double", "double", "string", "string", "string", "string"];

% Specify file level properties
opts.ExtraColumnsRule = "ignore";
opts.EmptyLineRule = "read";
opts.ConsecutiveDelimitersRule = "join";
opts.LeadingDelimitersRule = "ignore";

% Specify variable properties
opts = setvaropts(opts, ["Var3", "Var4", "Date", "Start_time"], "WhitespaceRule", "preserve");
opts = setvaropts(opts, ["Var3", "Var4", "Date", "Start_time"], "EmptyFieldRule", "auto");

% Import the data
tbl = readtable(filename, opts);
end