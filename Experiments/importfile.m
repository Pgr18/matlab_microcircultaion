function rcm = importfile(filename, dataLines)
%IMPORTFILE Import data from a text file
%  RCM = IMPORTFILE(FILENAME) reads data from text file FILENAME for the
%  default selection.  Returns the data as a table.
%
%  RCM = IMPORTFILE(FILE, DATALINES) reads data for the specified row
%  interval(s) of text file FILENAME. Specify DATALINES as a positive
%  scalar integer or a N-by-2 array of positive scalar integers for
%  dis-contiguous row intervals.
%
%  Example:
%  rcm = importfile("/Users/brikoan/Library/Containers/Mail.Ru.DiskO.as/Data/Disk-O.as.mounts/brikoan@mail.ru-mailru/РНЦХ/Проектный офис/Микроциркуляция/Эксперименты/G/DATA/2024-07-05/2024-07-05 08-41-45 852 rcm.csv", [2, Inf]);
%
%  See also READTABLE.
%
% Auto-generated by MATLAB on 19-Jul-2024 14:02:03

%% Input handling

% If dataLines is not specified, define defaults
if nargin < 2
    dataLines = [2, Inf];
end

%% Set up the Import Options and import the data
opts = delimitedTextImportOptions("NumVariables", 8);

% Specify range and delimiter
opts.DataLines = dataLines;
opts.Delimiter = ",";

% Specify column names and types
opts.VariableNames = ["TIME", "RHEO1", "BASE1", "QS1", "ECG", "RHEO2", "BASE2", "QS2"];
opts.VariableTypes = ["double", "double", "double", "double", "double", "double", "double", "double"];

% Specify file level properties
opts.ExtraColumnsRule = "ignore";
opts.EmptyLineRule = "read";

% Import the data
rcm = readtable(filename, opts);

end